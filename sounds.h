#ifndef SOUNDS_H
#define SOUNDS_H

#include <stdint.h>
#include <stddef.h>

extern const uint16_t button_error_notes[];
extern const uint16_t button_error_durations[];
extern const size_t button_error_length;

extern const uint16_t button_confirm_notes[];
extern const uint16_t button_confirm_durations[];
extern const size_t button_confirm_length;

#endif // SOUNDS_H